{
  "name": "ridiculist",
  "private": true,
  "version": "0.0.0",
  "description": "Ridiculously simple lists, for everything.",
  "repository": "https://github.com/dev-coop/ridiculist-webapp",
  "license": "MIT",
  "devDependencies": {
    "karma": "~0.10",
    "protractor": "^1.1.1",
    "bower": "^1.3.1",
    "karma-junit-reporter": "^0.2.2",
    "live-server": "^0.7.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "postsetup": "npm start", 
    "start": "$(npm bin)/live-server app",
    "server": "$(npm bin)/live-server app",
    "pretest": "npm install",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js  --single-run",
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update",
    "preprotractor": "npm run update-webdriver",
    "protractor": "protractor e2e-tests/protractor.conf.js"
  },
  "dependencies": {
  }
}
